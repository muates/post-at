import { PostMediaResponse } from './post-media-response';

describe('PostMediaResponse', () => {
  it('should create an instance', () => {
    expect(new PostMediaResponse()).toBeTruthy();
  });
});
